import 'package:flutter/material.dart';

class AppColors {
  // ── Primary Brand ──────────────────────────────────────────
  static const Color primary = Color(0xFFFF6B35);
  static const Color primaryLight = Color(0xFFFF8C42);
  static const Color primaryDark = Color(0xFFE05200);

  // ── Non-Veg Red variants ───────────────────────────────────
  static const Color nonVegPrimary = Color(0xFFDC2626);
  static const Color nonVegPrimaryLight = Color(0xFFEF4444);
  static const Color nonVegPrimaryDark = Color(0xFFB91C1C);

  // ── Backgrounds ────────────────────────────────────────────
  static const Color background = Color(0xFFFFF8F0);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceWarm = Color(0xFFFFF3EC);

  // ── Text ───────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF3D3D3D);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textWhite = Color(0xFFFFFFFF);

  // ── Status ─────────────────────────────────────────────────
  static const Color success = Color(0xFF16A34A);
  static const Color error = Color(0xFFDC2626);
  static const Color warning = Color(0xFFD97706);

  // ── Borders & Dividers ─────────────────────────────────────
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);

  // ── Shadows ────────────────────────────────────────────────
  static const Color shadow = Color(0x14000000);

  // ── Payment Colors ─────────────────────────────────────────
  static const Color upiPurple = Color(0xFF7C3AED);
  static const Color upiPurpleLight = Color(0xFFEDE9FE);
  static const Color codYellowBg = Color(0xFFFEF9C3);
  static const Color codYellowIcon = Color(0xFFCA8A04);

  // ── Semantic / Extra ───────────────────────────────────────
  static const Color savingsGreen = Color(0xFF16A34A);
  static const Color accepted = Color(0xFF2563EB);
  static const Color revenue = Color(0xFF0891B2);
  static const Color warningBg = Color(0x55D97706);

  // ── Legacy aliases (purani files tootein nahi) ─────────────
  static const Color card = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color accent = Color(0xFFFF6B35);
  static const Color gold = Color(0xFFFFA500);
  static const Color mutedText = Color(0xFF6B7280);
  static const Color muted = Color(0xFF6B7280);

  // ── Admin / Extra aliases ──────────────────────────────────
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color acceptedBlue = Color(0xFF2563EB);
  static const Color revenueCyan = Color(0xFF0891B2);
  static const Color upiLight = Color(0xFFEDE9FE);
}

class AdminColors {
  static const Color background = Color(0xFFF5F6FA);
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF7A7F9A);
  static const Color success = Color(0xFF00C48C);
  static const Color error = Color(0xFFFF4757);
  static const Color warning = Color(0xFFFFAB00);
  static const Color infoBlue = Color(0xFF2979FF);
}
